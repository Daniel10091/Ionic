import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'auto.fuel',
  appName: 'auto-fuel',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
